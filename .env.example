# The Ogen Complex — Environment Variables
# SEALED STATE | Version 1.0.0-sealed

# REQUIRED: Cryptographic seed for RS256 key generation
# Never commit the actual value. Set in deployment secrets.
OGEN_SEED=your-secret-seed-here

# Server
PORT=8080
NODE_ENV=development

name: Central Deploy

on:
  workflow_call:
    # This allows the workflow to be called by other repositories

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy
        run: |
          echo "Deploying from tx-ogenmix..."
          # Add your actual deployment commands here
          echo "✅ Deployment complete!"
